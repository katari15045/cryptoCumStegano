1. After sending OTP, a new screen appears with wierd dimensions to take OTP from user

2. Public key shouldn't be choosen by the user and should be sent just by taking receivers_email_address

3. While sending email, a dialog box with "sending email..." should appear
